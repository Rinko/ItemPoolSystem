<%= form_for(@student) do |f| %>
  <% if @student.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@student.errors.count, "error") %> prohibited this student from being saved:</h2>

      <ul>
      <% @student.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <p>
    <%= f.label :progress_info %><br />
    <%= f.text_field :progress_info %>
  </p>
  <p>
    <%= f.label :answered_questions_info %><br />
    <%= f.text_field :answered_questions_info %>
  </p>
  <p>
    <%= f.label :knowledge_point_info %><br />
    <%= f.text_field :knowledge_point_info %>
  </p>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
